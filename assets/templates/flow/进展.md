# 进展日志(append,新的放最上面)

> 每个 Agent 处理完一棒,在**最上面追加一条**。**顶部那条 = 当前交接棒**,下一棒读它接上。
> 这是「可复制、可回看」的接力记录:复制一条喂别的 Agent,或从上往下扫看全历史。
> 决策 → `decisions.md`,坑 → `踩坑记录.md`,内容产物 → `docs/`·`scripts/`(这里只给路径,不重抄)。

<!-- 模板(复制到最上面填)。标题必须用 `## `，GC 按它分页。

## YYYY-MM-DD · <任务 / 这一棒> · <谁:Claude / Codex> · thread=<CODEX_THREAD_ID>
- 来源会话：thread=<CODEX_THREAD_ID>（必填）
- 规格点(SPE)：SPE-1..n —— 只写「要执行的规格」，不抄用户原话
- 待办(TODO)：- [ ] 未做 / - [-] 进行中 / - [x] 完成 / - [!] 阻塞；完成项必须带证据
- 现状: <已经做到哪，具体到文件路径或已通过的命令，不写背景>
- 还剩: <未回收的 SPE 编号，以及为什么没做完>
- 卡在哪: <熔断原因 / 缺什么 / 无>
- 下一步: <接手方第一条具体命令>
- 台账: flow/specs/<ticket>.md（未回收项是唯一待办源）

体积口径（2026-09-23 按 229 条真实交接棒实测重标）：p50 1543 / p90 2646 字节，
硬上限 4000。旧写法「严格 800 字节内」与实测不符（72% 超 1200），已废止——
但「不许复述背景」仍然成立，且由照抄检测把关（最长连续照抄 >= 400 字符即判不合格）。
-->
