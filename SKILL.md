---
name: project-flow-cy
description: 用一套基于文件的多 Agent 协作流程来初始化和管理项目,并在 Claude Code 与 Codex 之间用 flow/进展.md 进展日志接力。核心机制——(1) 任务四状态闭环流转: 待完成[](当前会话P0优先执行)、待验收[-](静默待确认不复读)、已完成[✓](彻底完结物理归档)、不合格[✕](立即重构优先修复); (2) 任务按需加载: 严禁无脑塞入全量历史任务，仅加载当前焦点切片，杜绝上下文污染与乱入需求; (3) 单仓多子项目分层接入与轻量 Fast Track 通道。
---

# project-flow-cy (四状态机与按需加载版)

把任何项目(代码 / 调研 / 内容 / 方案)按清晰的**项目边界、四状态生命周期与按需加载机制**来管理。
**上下文落进文件，Agent 之间靠 `flow/`、`docs/` 异步接力，跨模型评审做质量门。杜绝历史任务反复执行、杜绝上下文污染、杜绝乱入需求干扰。**

- 完整方法论详规在 `references/`(`工作流程.md` / `任务状态机与按需加载SOP.md` / `多子项目结构.md` / `文档维护SOP.md` / `DESIGN维护SOP.md` / `hook机制.md` / `初始化SOP.md`)。
- 注入项目的模板实体在 `assets/templates/`。

## 先判断用户要哪个操作
- 要"初始化 / 接入 / 搭结构 / 建骨架" → **操作 A**
- 要"收工 / 交接 / 写交接卡 / handoff / 交接给下一个" → **操作 B**(在进展日志追加一条)
- 要"任务状态流转 / 验收通过归档 / 缺陷重构" → **操作 C**
- 拿不准就先问一句,别猜。

---

## 🎯 核心机制一: 任务四状态机与优先级调度 (Task State Machine)

所有任务严格按以下四种状态流转，严禁越界或死循环执行：

| 状态标记 | 状态名称 | 优先级 | Agent 行为准则 |
|:---|:---|:---|:---|
| **`[ ]`** | **待完成 (TODO)** | **最高 (P0) / 排队** | **当前会话用户指定任务优先级最高**，立即执行；其余待办按顺序排队，不抢占当前焦点。 |
| **`[-]`** | **待验收 (Pending Verification)** | **静默挂起 (不执行)** | **上一轮已执行完成、等待用户验收确认**。Agent **绝对不要去重复执行、不要反复排查、不要去管它**！ |
| **`[✓]`** | **已完成 (Done)** | **已终结 (Task GC)** | **用户确认合格，彻底完结**。立即物理移出活跃 `flow/plan.md`，归档至 `flow/archive/`，不留文本残留。 |
| **`[✕]`** | **不合格 (Rejected)** | **最高 (P0 重构)** | **用户验收判定不合格**，指出缺陷。Agent 本次会话**必须立即针对不合格项进行重构/修改**，完成后转为待验收 `[-]`。 |

### 闭环流转路径：
```
[ ] 待完成 (用户下达/会话最高优先)
  ↓ 执行完成
[-] 待验收 (静默等待用户反馈，Agent严禁自发重跑)
  ├── 用户判定合格 ──→ [✓] 已完成 ──→ 物理移入 flow/archive/ (彻底终结)
  └── 用户判定不合格 ─→ [✕] 不合格 ──→ 立即作为 P0 优先修复修改 ──→ 返回 [-] 待验收
```

---

## ⚡ 核心机制二: 任务按需加载机制 (On-Demand Loading)

**严禁将所有历史任务、全量背景一次性塞进上下文！**
1. **只读活跃焦点切片**: Agent 开工时，仅加载 `flow/plan.md` 中当前会话正在执行的 1~2 个活动任务（`[ ]` 或 `[✕]`）；
2. **待验收区静默隔离**: `[-] 待验收` 仅作为看板展示，Agent 读取时自动忽略，不将其中涉及的历史代码逻辑作为待办处理；
3. **归档区物理隔离**: `[✓] 已完成` 任务物理移入 `flow/archive/`，开工时不加载归档文件，彻底切断历史文本对模型注意力的干扰；
4. **杜绝乱入需求**: 除非用户在当前对话中明确提出关联，否则不主动加载与当前焦点任务无关的其它模块上下文。

---

## 核心操作

### 操作 A: 初始化 / 接入项目
把当前项目接入协作流程,产出标准结构。**完整步骤与验证清单在 `references/初始化SOP.md`——执行前先读它。** 要点:
1. **先判项目边界**:扫描当前目录代码入口与配置，判断是单体项目、单仓多子项目(monorepo)还是多个独立仓库。一个项目边界一个控制面，多子项目先读 `references/多子项目结构.md`。
2. **判断状态**:目录是否已有 `AGENTS.md` / 内容。新项目全量铺;已有项目非破坏合并(缺啥补啥,绝不覆盖已有内容)。
3. **先列清单等确认**:把项目边界判断和"将创建 / 修改的文件"列给用户,确认后再动手。
4. **铺根级骨架**:每个项目边界只建一套 `flow/`(`charter.md` `plan.md` `进展.md` `decisions.md` `踩坑记录.md` `tasks/` `archive/`)+ `docs/`。
5. **生成分层入口**:用 `assets/templates/AGENTS.md` 生成根级 `AGENTS.md`,再让根级 `CLAUDE.md` 指向它。单仓多子项目中，每个有独立职责的子项目用 `assets/templates/MODULE_AGENTS.md` 生成局部 `AGENTS.md` 并建立同目录 `CLAUDE.md` 软链(Windows 改复制);局部入口只写本模块职责、命令、约束和根级指针,不复制总体计划。可选 `DESIGN.md` 仅用于设计 / 创意工作。
6. **装 hook**:只在每个项目边界根目录复制 `assets/templates/.hooks/`、`.claude/`、`.codex/`;`chmod +x .hooks/stop-doccheck.sh`。Claude Code 只以 `prompt_id`、可解析版本不低于 `0.145.0` 的 Codex 只以 `turn_id` 界定用户回合,每回合自动续跑一次;更旧或未知版本安全放行。
7. **详规随项目**:把 `references/` 下详规复制到根级 `flow/规范/`(自包含)。
8. **跑接入自检**:运行 `bash tests/test-stop-hook.sh` 与 `bash tests/test-multi-project-structure.sh`。

### 操作 B: 收工交接 (进展日志)
把当前会话这一棒的进展,**追加到 `flow/进展.md` 最上面**,供下一个 Agent / 会话接力。顶部那条 = 当前交接棒。
1. 在 `flow/进展.md` 顶部加一条，包含：做了什么 / 为什么 / 怎么理解 / 产出(真实路径)/ 问题→解决 / 下一步。并把这条同时贴在回复中。
2. 决策追加 `flow/decisions.md`;问题 / 踩坑追加 `flow/踩坑记录.md`。
3. **铁律:只带「指针 + 增量」**——给真实文件路径，不重抄全量内容。

### 操作 C: 任务状态流转与即时物理回收 (Task GC)
1. 任务完成 -> 状态置为 `[-] 待验收`；
2. 用户验收通过 -> 状态置为 `[✓] 已完成` -> **立即物理剪切移入 `flow/archive/`**；
3. 用户验收不通过 -> 状态置为 `[✕] 不合格` -> 立即作为当前会话 P0 任务修复。

---

## 一直要守的铁律
1. **用户即时指令第一优先级**: 当前对话明确诉求 > 历史 plan。
2. **待验收 `[-]` 绝对不重跑**: 上轮已交付项静默等待用户确认，绝不自我感动式重复排查。
3. **不合格 `[✕]` 立即就地修复**: 用户指出问题，以最高优先级当场改到位。
4. **已完成 `[✓]` 物理零留存**: 验收通过即剪切移出活跃区，保持 plan.md 极致清爽。
5. **按需加载**: 不读无关历史，一会话一焦点。
6. **一个项目边界一个控制面**: 单仓多子项目只保留一套根级 `flow/`、`docs/` 和 hook，不在子项目机械复制。
7. **目录归属**: 协调 / 推进项目的 → 根级 `flow/`;跨模块知识、方案和交付说明 → 根级 `docs/`;代码 → 对应子项目目录。
