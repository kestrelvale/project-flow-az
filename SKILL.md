---
name: project-flow-az
description: "生产级战略驱动设计多 Agent 协作工作流 (V4.9.3 完整收工汇报版)。融合奥卡姆剃刀、Ask-Matt (Smart Zone/单票独立会话)、孙子兵法 (兵贵神速/避实击虚) 与矛盾论 (主要矛盾第一/代码落地优先) 四大哲学内核."
---

# project-flow-az (生产级战略驱动设计 V4.9.3 完整收工汇报版)

把任何项目(代码 / 调研 / 内容 / 方案)按清晰的**四大哲学支柱（奥卡姆剃刀/Ask-Matt/孙子兵法/矛盾论）与四大先进驱动设计流水线（SDD ➔ TDD ➔ ATDD ➔ BDD）**来管理。
**上下文落进文件，Agent 之间靠 `flow/`、`docs/` 异步接力，跨模型评审做质量门。彻底杜绝历史任务反复加载、杜绝上下文爆炸截断、杜绝过度探查死循环、杜绝以文代码假完工。**

- 完整方法论详规在 `references/`(`工作流程.md` / `任务状态机与按需加载SOP.md` / `Sub-Agent多Agent并行协作SOP.md` / `任务回收与归档SOP.md` / `自动版本接管与无感热更新SOP.md` / `多子项目结构.md` / `文档维护SOP.md` / `DESIGN维护SOP.md` / `计划模式交接SOP.md` / `驱动模式与验证策略.md` / `hook机制.md` / `初始化SOP.md`)。
- 注入项目的模板实体在 `assets/templates/`。

## 先判断用户要哪个操作
- 要"初始化 / 接入 / 搭结构 / 建骨架" → **操作 A**
- 要"收工 / 交接 / 写交接卡 / handoff / 交接给下一个" → **操作 B**(在进展日志追加一条，仅留1~2条)
- 要"任务状态流转 / 提请验收 / 验收归档 / 缺陷重构" → **操作 C**
- 运行中检测到版本落后 → **操作 D (自动执行无感热同步，无需用户操心)**
- 拿不准就先问一句,别猜。

---

---

## 🎯 核心机制零: 用户当面指令唯一触发与活态纠偏原则 (Sole Trigger & Living Steering)

**彻底杜绝“把清单当执行指令、自发顺带执行未授权任务”及“僵化死跑忽视用户打断”的双重工程顽疾！**
1. **清单 ≠ 执行指令，活人 Prompt 高于一切**:
   - `flow/plan.md` 是随手记事的动态白板，绝非束缚行动的铁锁链；
   - **只有用户在当轮回合明确提到、显式要求执行的任务，才是当前施工目标**；
   - 未点名清单项 100% 绝对静默，严禁自发顺带修改未授权模块。
2. **主任务恒定推进原则 (Maintain Master Mission)**:
   - 用户的中途插话属于**“航向纠偏与参数校准”**（如指定测试版、补充 MCP），绝非推翻主任务；
   - 主任务目标恒定推进，严禁因一条微调提示就抛弃整体任务；唯有用户明确发出“做得不对，完全给我废弃掉/停止”时方可终止主任务。
3. **人机协同测试与角色让位法则 (Human Co-Testing & Role Yielding)**:
   - 凡用户提出“我可以测试了吗”、“让我来点/测试”、“你帮我记日志/监控”等信号：
     - **Agent 必须无条件立即停止所有自主业务接口调用与数据写入！**
     - 立即将操作主导权移交人类，Agent **当场转为【只读日志观察员】**，绝对严禁抢在用户前面去跑业务接口或篡改订单状态；
     - 确认后台服务与日志正常监听，给出极简指引（“服务与日志已就位，请您在端上操作，我正在实时监控”），静默等待人类触发。
4. **动态纠偏与即时注入**:
   - 用户指出“应向这个方向做”或投喂了工具/配置/参数（如补充 MCP）：**必须立即吸收并融入当前主任务路径继续执行**。
5. **动态追加待办**:
   - 用户提出“还要加个什么东西”：立即将增量项追加进 `flow/plan.md` 待办列表，合并规划推进。
6. **两重试熔断与遇阻透明汇报 (Blocker Escalation Gate)**:
   - 任何构建、配置、环境或发布操作，**连续尝试 2 次走不通，必须强制立即停手！**
   - **严禁采用旁门左道**（如编写 AppleScript 抓瞎乱点屏幕、强行修改系统级进程等高危操作）；
   - 必须立即向用户输出简明汇报说明阻塞点，提请用户决策，绝不允许一股脑走到黑！

---

## 🎯 核心机制一: 任务四状态机与 SDD 规格驱动原子拆解 (Task State Machine & SDD)

所有任务严格按以下四种状态流转，**任何实质性回复的第一屏必须强制输出独立代码块 To-Do 看板与提请验收卡**：

| 状态标记 | 状态名称 | 优先级 | Agent 行为准则 |
|:---|:---|:---|:---|
| **`[ ]`** | **待完成 (TODO)** | **最高 (P0) / 立即执行** | **用户当前会话下达的任务为唯一最高优先级**。复合多子项需求必须按 **SDD 拆解为原子 To-Do (P0-1, P0-2...)**，锁死 I/O 契约与写入边界白名单，立即进入执行。 |
| **`[-]`** | **待验收 (Pending)** | **静默挂起 (不执行)** | **代码已落盘且通过专属自动化微单测 (<3s, Exit 0, 0 Error, 截图存盘)**，等待用户确认。Agent **绝对不要去重复执行、不要反复排查、不要去管它**！ |
| **`[✓]`** | **已完成 (Done)** | **已终结 (Task GC)** | **用户确认合格，彻底完结**。立即在当回合第一指令物理剪切移出 `flow/plan.md`，归档至 `flow/history/`，活跃区零留存。 |
| **`[✕]`** | **不合格 (Rejected)** | **最高 (P0 重构)** | **用户验收判定不合格**，指出缺陷。Agent 本次会话**必须立即针对不合格项进行重构/修改**，修复后转为待验收 `[-]`。 |

### ⚡ 独立代码块 To-Do List 看板协议 (Code-Block Checklist Protocol):
```markdown
### 📊 任务状态看板
# 任务状态看板

## 🎯 当前聚焦待办 (P0 任务组 - SDD 规格拆解)
- [ ] P0-1 [规格一] <原子任务描述 1> (白名单边界: path/to/fileA)
- [ ] P0-2 [规格二] <原子任务描述 2> (白名单边界: path/to/fileB)

## ⏳ 待人工验收 (Pending Verification)
- [-] <已完成自测交付，等待用户确认> [🤖 自动测试通过][👤 待人工验收]

## 📦 已完结归档 (Archived in flow/history/)
- [✓] <已剪切物理隔离至 flow/history/ 的任务简述>

## 💡 本轮决策记录 (Decisions)
- [⚡ 自动决策] <内部技术选型/实现细节，已记录于 decisions.md>
- [⚠️ 人工决策门] <极高风险/重大分歧提请确认>
```

👉 **提请验收卡 (BDD 场景化体验步骤)**:
- **改动文件**: `path/to/file.html` (L120-L145)
- **🤖 自动自验**: 专属脚本 `scripts/test_xxx.js` PASS (耗时 < 3s, Exit 0, 0 Error, 截图已留盘)
- **👤 待人工体验 (Given-When-Then)**:
  - **Given**: 使用测试账号登录系统并进入【目标模块页面】；
  - **When**: 点击【操作按钮 / 触发交互】；
  - **Then**: 页面顺畅弹出【目标弹窗/更新数据】，无报错，状态持久化落盘。

---

## ⚡ 核心机制二: 任务按需加载与动态销账 (On-Demand Loading & Dynamic Pruning)

1. **SDD 契约锁死与范围白名单**: 在 `plan.md` 认领任务时，用 1 句话锁死“改哪个文件、输入输出字段是什么”，不准发散；
2. **多子要点逐项销账 (In-Prompt Pruning)**: 复合需求每完成 1 个子项即打标 `[✓ P0-x DONE -> PRUNED]` 并从注意力切片中剥离，后续轮次仅保留未完成项；
3. **多端分卡与白名单切片 (Domain Queueing)**: 单仓多端项目的待验收任务卡分散存放在 `flow/tasks/<模块>.md` 独立排队。当前会话做特定端时**绝对禁止读取无关模块的任务卡**；
4. **指针交付原则 (Pointers Only)**: 凡超 20 行长文一律直接写入文件，聊天窗口仅输出极简看板、核心结论（≤20行）与文件绝对路径；
5. **归档区物理隔离**: `[✓] 已完成` 任务物理移入 `flow/history/`，废弃方案移入 `flow/trash/`，开工时不加载隔离文件。

---

## ⚡ 核心机制三: 全局 Skill 自动热同步与版本接管 (Auto Hot-Sync & Version Takeover)

**彻底解决全局 Skill 更新后历史项目规则落后的问题！**
1. **触发即嗅探**: 在任何项目开工或执行指令时，比对项目本地 `flow/规范/VERSION` 与全局 Skill 的 `VERSION`；
2. **非破坏自动热同步 (Auto Hot-Sync)**:
   - 自动覆盖更新 `flow/规范/` 下的方法论详规；
   - 自动补齐缺失的 `flow/history/`、`flow/tasks/` 与 `flow/trash/` 目录；
   - 增量更新 `AGENTS.md` 中的规范标记块，使项目立即具备最新状态机与决策能力；
   - 绝对保留用户原有的业务代码、业务规则与历史进展数据；
3. **零阻碍感知**: 仅在首屏输出 1 行提示：`💡 [project-flow-az] 检测到全局 Skill 升级，已自动为当前项目热同步至 v4.0.0 规范体系。`

---

## ⚡ 核心机制四: 高风险人工决策升级门 (High-Risk Bounded Escalation Gate)

1. **自动决策是默认常态**: 日常代码实现、函数封装、CSS 样式、局部重构、异常防御一律由 Agent 自主决策并记入 `flow/decisions.md`，**严禁滥用人工决策打断用户**；
2. **严格触发人工决策卡 (仅限极高风险或用户明确要求)**:
   - **极高风险/破坏性改动**: 删除核心未备份数据、涉及线上支付/结算/鉴权安全、废弃生产公共 API 契约；
   - **用户明确要求**: 用户在 Prompt 中要求“让我确认后再改”、“列出方案让我选”；
   - **架构颠覆与主线废弃**: 引入重型新框架、根级目录重构、或将主干功能废弃移入 `flow/trash/`；
3. 触发时输出【人工决策卡】并暂停等待用户选择。

---

## 🤖 核心机制五: Sub-Agent 蜂群架构与双引擎自适应降级 (Sub-Agent Swarm)

1. **主控 Agent (Orchestrator) 统揽大局**:
   - 维护唯一的 `flow/plan.md` 和 `flow/进展.md` 控制面；
   - 负责需求切片拆解，分配互不交叠的写入文件集合（Disjoint Write Sets）；
   - 调用 `spawn_agent` 派发专用子 Agent（前端 Worker / 后端 Worker / 测试 Reviewer / 深度研读 Explorer）；
   - 收集子 Agent 交付物，本地统一编译验证并执行 GC 归档。
2. **子 Agent (Worker Sub-Agents) 隔离执行**:
   - 运行在轻量专属上下文中，绝不将数万行历史杂音带入子任务；
   - **子 Agent 严禁读写 `flow/` 目录**，完工后向主控交付 `files_modified` 与测试结论；
3. **代码落地与收工自检双重硬门禁 (Execution Completeness Gate)**:
   - **禁止文档代代码**：功能类任务必须以实体代码落盘与测试通过为完结依据；
   - **自检反查拦截**：准备收工时，主控 Agent 主动反查子要点与 Sub-Agent 完工状态；有未完成项必须继续编码，彻底杜绝半路偷懒收工！
4. **双引擎自适应降级 (Dual-Engine Fallback)**:
   - 当运行环境支持 `spawn_agent` 时，自动走原生沙箱并发；
   - 当上游中继未开放 multi-agent 原生接口时，自动无感降级为 Side Chat 模式（建议用户在侧边栏开 2~3 个独立 Task 线程并行执行）或主控严格切片动态销账模式，保证在任何环境下均能稳定闭环！

---

## 🚦 核心机制六: 会话健康度监控与自动接力预警 (Session Relay & Context Health Gate)

**彻底解决长会话 Token 膨胀引发模型语法退化与截断的顽疾！**
1. **单票独立会话原则 (One-Ticket-One-Session)**:
   - 复杂多模块任务拆为独立 Ticket 分会话执行，每个 Ticket 做完落盘并自测后，该会话上下文即废弃 (Disposable Context)；
   - 下一个 Ticket 开启全新会话，读取顶部最新 1 条接力棒，**永远保持在 5k ~ 20k Token 黄金 Smart Zone 内作业**。
2. **主动接力与阈值熔断**:
   - 主控 Agent 根据上下文长度、工具调用数量和任务边界主动判断是否换会话；
   - 换会话前必须将最新接力记录写入 `flow/进展.md`，并提示用户读取顶部记录继续。
3. **Gemini 零缓存中转硬熔断红线 (Gemini Zero-Cache Hard Ceiling)**:
   - **触发前提**: 当调用模型包含 `gemini` 时（如 `gemini-3.8-flash-high`、`gemini-3.7-flash-high` 等走中转路由链路）。因当前上游中转链路缓存命中率实测为 0%，长会话全量重复计费导致费用呈 10~12 倍暴增；
   - **硬上限红线**: 单会话交互轮次 **严格 ≤ 25 轮**、累计上下文 **严格 < 10 万 Tokens**、单任务工具调用 **严格禁止超过 100 次**；
   - **超限阻断行为**: 一旦达到 20 轮或估算上下文接近 10 万 Tokens，**必须立即停止推进新的实现代码**，将当前阶段成果落盘至真实文件，向 `flow/进展.md` 追加最新接力记录，并在当前回复第一屏强制输出【会话超限熔断与接力提示词卡】，提示用户直接复制提示词、开启新会话继续执行；**严禁在已超限的 Gemini 会话中强行继续执行复杂长任务**。

---

## 核心操作

### ⚡ 强制执行铁律 (Step 0: 开工入口)
已接入项目在每个执行型回合的第一次工具调用必须执行：
```bash
python3 ~/.codex/skills/project-flow-az/scripts/flow-boot.py . --intent "<本轮任务摘要>"
```
该入口向上定位项目、按需热同步、执行只读审计、输出接管路由并执行会话预算门禁。必须消费活跃焦点、Plan/Execute/Handoff 路由、静默卡、“本轮意图未登记”和 `flow-budget.py` 的 WARN/STOP 结果；发现 FAIL/WARN、路由阻塞、未登记任务或预算 STOP 时，先补齐 SDD 登记、落盘并写交接棒，再决定是否开新会话。`flow-sync.sh` 仅作为内部同步器，不作为开工入口。

#### 阶段与驱动模式唯一映射（不跳阶段，不混用）

| 项目阶段 | 计划模式 | 驱动模式 | 任务卡 `mode` | 阶段职责 |
|---|---|---|---|---|
| 项目初期 | Plan / Goal | SDD | `plan` | 定目标、锁规格、拆原子任务；只读，不改业务代码 |
| 实现期 | - | TDD | `execute` | 先写失败测试，再最小实现使其通过 |
| 验收期 | - | ATDD | `review` | 把验收标准转成可执行断言并留证据 |
| 收尾期 | - | BDD | `handoff` | Given-When-Then 描述行为，交接下一步动作 |

单向流转 `plan -> execute -> review -> handoff`：任务卡以 `goal` 表达目标（旧卡兼容 `objective`），进入下一阶段只改 `mode`/`method` 并补齐该阶段必填字段，再由 `flow-gate.py --phase <阶段>` 放行。注意：本流程的 `plant` 指 Plan（计划模式），不是 PlantUML 等图示工具。

---

### 操作 A: 初始化 / 接入项目
把当前项目接入协作流程,产出标准结构。**完整步骤与验证清单在 `references/初始化SOP.md`。**
1. 扫描当前目录代码入口与配置，判断单体项目、单仓多子项目还是多仓；
2. 铺根级骨架：`flow/`(`charter.md`, `plan.md`, `进展.md`, `decisions.md`, `踩坑记录.md`, `tasks/`, `history/`, `trash/`, `规范/`) + `docs/`；
3. 仅装载 `flow/` 与 `docs/` 控制面，不安装项目级 Hook；
4. 跑接入自检测试。

### 操作 B: 收工交接 (进展日志)
把当前会话这一棒的进展追加到 `flow/进展.md` 最上面（严格滚动保留最新 1~2 条）。包含：做了什么 / 为什么 / 怎么理解 / 产出路径 / 问题→解决 / 下一步。

### 操作 C: 任务状态流转与即时物理回收 (Task GC)
1. 任务完成 -> 状态置为 `[-] 待验收` 并输出包含 **Given-When-Then** 的【提请验收卡】；
2. 用户验收通过 -> 状态置为 `[✓] 已完成` -> **立即在当回合第一动作物理剪切移入 `flow/history/`**；
3. 用户验收不通过 -> 状态置为 `[✕] 不合格` -> 立即作为当前会话 P0 任务修复。

### 操作 D: 自动热同步与版本接管 (Auto Hot-Sync)
开工时比对 `flow/规范/VERSION`。若发现版本落后，自动执行无感增量热同步，并升级至最新版本号。

---

## 一直要守的铁律
1. **用户即时指令第一优先级**: 当前对话明确诉求 > 历史 plan。
2. **待验收 `[-]` 绝对不重跑**: 上轮已交付项静默等待用户确认，绝不自我感动式重复排查。
3. **不合格 `[✕]` 立即就地修复**: 用户指出问题，以最高优先级当场改到位。
4. **已完成 `[✓]` 物理零留存**: 验收通过即剪切移出活跃区，保持 plan.md 极致清爽（<200 Tokens）。
5. **探查预算制 (≤3步)**: 禁止盲目全盘扫盘，CodeGraph 定位后立即改代码。
6. **代码先行 (Code-First)**: 严禁以方案报告代替代码落地，必须有实际文件修改与微测试通过。
7. **本地先行与受控发版**: 所有问题在本地修改验证，严禁自动推线上，必须等显式发版指令。
8. **单票独立会话与 8 轮熔断**: 复杂任务分会话推进，超 8 轮提示开新 Task 接力，防 Token 爆炸。
9. **Gemini 零缓存中转硬熔断红线**: 只要使用 Gemini 模型（中转链路 0% 缓存），必须死守 **≤25 轮、<10 万 Tokens、<100 次工具调用** 红线；超限强制禁止继续编码，落盘成果并输出接力提示词引导切换新会话。
10. **一个项目边界一个控制面**: 单仓多子项目只保留一套根级 `flow/` 与 `docs/`，用 `assets/templates/MODULE_AGENTS.md` 生成局部入口，不在子项目机械复制。
11. **轻量边界**: project-flow 不负责 Hook 触发与无人值守定时；定时任务必须由外部 automation 显式调用。
