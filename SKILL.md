---
name: project-flow-az
description: 用一套基于四状态机(待完成[]/待验收[-]/已完成[✓]/不合格[✕])、按需加载、自动热同步与高风险人工决策门的多 Agent 协作体系。支持 project-flow-az 与 project-flow-cy 双别名调用。在 Claude Code 与 Codex 之间用 flow/进展.md 接力。核心机制——(1) 任务四状态闭环流转: 待完成[](当前会话P0优先执行)、待验收[-](静默待确认不复读)、已完成[✓](彻底完结物理归档)、不合格[✕](立即重构优先修复); (2) 任务按需加载与动态销账: 严禁无脑塞入全量历史任务，仅加载当前焦点切片，杜绝上下文污染与乱入需求; (3) 分级任务回收(自动轮转 vs 人工确认); (4) 高风险人工决策升级门: 自动决策为常态，仅极高风险或用户明确要求时触发人工决策卡; (5) 全局 Skill 自动热同步机制: 项目开工或触发时自动感知全局版本并无损升级接管当前项目; (6) 规划分域隔离与原路归流排队机制 (Domain Queueing & Return-to-Origin); (7) 单仓多子项目分层接入与轻量 Fast Track 通道。
---

# project-flow-az (四状态机、按需加载与自动热同步魔改版)

把任何项目(代码 / 调研 / 内容 / 方案)按清晰的**项目边界、四状态生命周期、分级回收、自动热同步与双轨决策机制**来管理。
**上下文落进文件，Agent 之间靠 `flow/`、`docs/` 异步接力，跨模型评审做质量门。杜绝历史任务反复执行、杜绝上下文污染、杜绝乱入需求干扰、杜绝项目规范落后于全局 Skill。**

- 完整方法论详规在 `references/`(`工作流程.md` / `任务状态机与按需加载SOP.md` / `任务回收与归档SOP.md` / `自动版本接管与无感热更新SOP.md` / `多子项目结构.md` / `文档维护SOP.md` / `DESIGN维护SOP.md` / `hook机制.md` / `初始化SOP.md`)。
- 注入项目的模板实体在 `assets/templates/`。

## 先判断用户要哪个操作
- 要"初始化 / 接入 / 搭结构 / 建骨架" → **操作 A**
- 要"收工 / 交接 / 写交接卡 / handoff / 交接给下一个" → **操作 B**(在进展日志追加一条)
- 要"任务状态流转 / 提请验收 / 验收归档 / 缺陷重构" → **操作 C**
- 运行中检测到版本落后 → **操作 D (自动执行无感热同步，无需用户操心)**
- 拿不准就先问一句,别猜。

---

## 🎯 核心机制一: 任务四状态机与首行看板协议 (Task State Machine & Header Protocol)

所有任务严格按以下四种状态流转，**任何实质性回复的第一屏必须强制输出【任务四状态机看板与提请验收卡】**：

| 状态标记 | 状态名称 | 优先级 | Agent 行为准则 |
|:---|:---|:---|:---|
| **`[ ]`** | **待完成 (TODO)** | **最高 (P0) / 排队** | **当前会话用户指定任务优先级最高**，立即执行；其余待办按顺序排队，不抢占当前焦点。 |
| **`[-]`** | **待验收 (Pending Verification)** | **静默挂起 (不执行)** | **上一轮已执行完成、等待用户验收确认**。Agent **绝对不要去重复执行、不要反复排查、不要去管它**！ |
| **`[✓]`** | **已完成 (Done)** | **已终结 (Task GC)** | **用户确认合格，彻底完结**。立即物理移出活跃 `flow/plan.md`，归档至 `flow/history/`，活跃区零留存。 |
| **`[✕]`** | **不合格 (Rejected)** | **最高 (P0 重构)** | **用户验收判定不合格**，指出缺陷。Agent 本次会话**必须立即针对不合格项进行重构/修改**，完成后转为待验收 `[-]`。 |

### ⚡ 独立代码块 To-Do List 看板协议 (Code-Block Checklist Protocol):
严禁使用 ASCII 字符画框（错位杂乱），任何实质性回复第一屏**必须使用独立 Markdown 代码块包裹的清晰 To-Do 清单格式**，分段列出二级标题与待办项，层次清晰绝不混淆：

````markdown
### 📊 任务状态看板
```markdown
# 任务状态看板

## 🎯 当前聚焦待办 (P0)
- [ ] <当前会话唯一核心执行目标>

## ⏳ 待人工验收 (Pending Verification)
- [-] <已完成自测交付，等待用户确认> [👤 待人工验收]

## 🤖 自动验收通过 (Auto-Verified)
- [✓] <已通过自动化脚本/测试用例 100% 跑通> [🤖 自动测试通过]

## 📦 已完结归档 (Archived in flow/history/)
- [✓] <已剪切物理隔离至 flow/history/ 的任务简述>

## 💡 本轮决策记录 (Decisions)
- [⚡ 自动决策] <内部技术选型/实现细节，已记录于 decisions.md>
- [⚠️ 人工决策门] <极高风险/重大分歧提请确认>
```

👉 **提请验收卡** (若本轮有实质交付):
- 交付产出: `<文件路径或访问地址>`
- 验收类型: `[🤖 自动测试通过 / 👤 待人工验收]`
- 核心要点: <1~2 句话总结改动与验证依据>
````

---

## ⚡ 核心机制二: 任务按需加载与动态销账 (On-Demand Loading & Dynamic Pruning)

1. **只读活跃焦点切片**: Agent 开工时，仅加载 `flow/plan.md` 中当前会话正在执行的 1~2 个活动任务（`[ ]` 或 `[✕]`）；
2. **多子要点逐项销账 (In-Prompt Pruning)**: 用户 Prompt 包含多个要点时，完成一项即打标销账并从注意力切片中剥离，后续轮次仅保留未完成项；
3. **规划分域隔离与防乱入 (Domain Boundary Guard)**:
   - 收到指令先核验工作区归属；非本仓库的外域临时问题，**严禁写入当前项目的 `flow/plan.md` 或 `flow/进展.md`**；
   - 单仓多端项目的规划按模块（如 C 端、B 端、运营后台）分流至 `flow/tasks/<模块>/` 独立排队，绝不混杂在根级主看板中。
4. **任务完成原路归流 (Return-to-Origin Routing)**:
   - 某个模块的任务完成后，原路更新该模块任务卡并提请验收；
   - 验收通过后归档到 `flow/history/tasks/<模块>/`，任务原路闭环，主看板 `flow/plan.md` 永远只保留当前激活的唯一 P0 焦点。
5. **待验收区静默隔离**: `[-] 待验收` 仅作为看板展示，Agent 读取时自动忽略，不将其中涉及的历史代码逻辑作为待办处理；
6. **归档区物理隔离**: `[✓] 已完成` 任务物理移入 `flow/history/`，废弃方案移入 `flow/trash/`，开工时不加载隔离文件。

---

## ⚡ 核心机制三: 全局 Skill 自动热同步与版本接管 (Auto Hot-Sync & Version Takeover)

**彻底解决全局 Skill 更新后历史项目规则落后的问题！**
1. **触发即嗅探**: 在任何项目开工或执行指令时，比对项目本地 `flow/规范/VERSION` 与全局 Skill 的 `VERSION`；
2. **非破坏自动热同步 (Auto Hot-Sync)**:
   - 自动覆盖更新 `flow/规范/` 下的方法论详规；
   - 自动补齐缺失的 `flow/history/` 与 `flow/trash/` 目录；
   - 增量更新 `AGENTS.md` 中的规范标记块，使项目立即具备最新状态机与决策能力；
   - 绝对保留用户原有的业务代码、业务规则与历史进展数据；
3. **零阻碍感知**: 仅在首屏输出 1 行提示：`💡 [project-flow-az] 检测到全局 Skill 升级，已自动为当前项目热同步至 v2.2.0 规范体系。`

---

## ⚡ 核心机制四: 高风险人工决策升级门 (High-Risk Bounded Escalation Gate)

1. **自动决策是默认常态**: 日常代码实现、函数封装、CSS 样式、局部重构、异常防御一律由 Agent 自主决策并记入 `flow/decisions.md`，**严禁滥用人工决策打断用户**；
2. **严格触发人工决策卡 (仅限极高风险或用户明确要求)**:
   - **极高风险/破坏性改动**: 删除核心未备份数据、涉及线上支付/结算/鉴权安全、废弃生产公共 API 契约；
   - **用户明确要求**: 用户在 Prompt 中要求“让我确认后再改”、“列出方案让我选”；
   - **架构颠覆与主线废弃**: 引入重型新框架、根级目录重构、或将主干功能废弃移入 `flow/trash/`；
3. 触发时输出【人工决策卡】并暂停等待用户选择。

---
---

## 🤖 核心机制五: Sub-Agent 多 Agent 并行派发与隔离协作体系 (Sub-Agent Swarm)

**彻底解决单 Agent 认知过载、大文件 OCR 塞爆上下文、多端模块协同慢的问题！**
1. **主控 Agent (Orchestrator) 统揽大局**:
   - 维护唯一的 `flow/plan.md` 和 `flow/进展.md` 控制面；
   - 负责需求切片拆解，分配互不交叠的写入文件集合（Disjoint Write Sets）；
   - 调用 `spawn_agent` 派发专用子 Agent（前端 Worker / 后端 Worker / 测试 Reviewer / 深度研读 Explorer）；
   - 收集子 Agent 交付物，本地统一编译验证并执行 GC 归档。
2. **子 Agent (Worker Sub-Agents) 隔离执行**:
   - 运行在轻量专属上下文中，绝不将数万行历史杂音带入子任务；
   - **子 Agent 严禁读写 `flow/` 目录**，完工后向主控交付 `files_modified` 与测试结论；
3. **代码落地与收工自检双重硬门禁 (Execution Completeness Gate)**:
   - **禁止文档代代码**：功能类任务必须以实体代码落盘与测试通过为完结依据；
   - **自检反查拦截**：收到 Stop Hook `【收工自检】` 时，强制反查子要点与 Sub-Agent 完工状态，有未完成项必须立即继续编码，彻底杜绝半路偷懒收工！

4. **双引擎自适应降级 (Dual-Engine Fallback)**:
   - 当运行环境支持 `spawn_agent` 时，自动走原生沙箱并发；
   - 当上游中继未开放 multi-agent 原生接口时，自动无感降级为 `create_thread` 独立子会话调度或主控严格切片动态销账模式，保证在任何环境下均能稳定闭环！

---

## 核心操作

### ⚡ 强制执行铁律 (Step 0: 物理热同步)
任何时候当用户发送 `用 /project-flow(...) 接入...` 或在项目中调用 `project-flow-az` / `project-flow-cy` 时，Agent **必须首先执行以下物理同步命令**，确保目标项目本地文件与全局 Skill 规范绝对一致：
```bash
bash ~/.codex/skills/project-flow-az/scripts/flow-sync.sh .
```
*严禁只在对话中口头回答，必须真实执行物理文件同步！*

---

### 操作 A: 初始化 / 接入项目
把当前项目接入协作流程,产出标准结构。**完整步骤与验证清单在 `references/初始化SOP.md`——执行前先读它。** 要点:
1. **先判项目边界**:扫描当前目录代码入口与配置，判断是单体项目、单仓多子项目(monorepo)还是多个独立仓库。一个项目边界一个控制面，多子项目先读 `references/多子项目结构.md`。
2. **判断状态**:目录是否已有 `AGENTS.md` / 内容。新项目全量铺;已有项目非破坏合并(缺啥补啥,绝不覆盖已有内容)。
3. **先列清单等确认**:把项目边界判断和"将创建 / 修改的文件"列给用户,确认后再动手。
4. **铺根级骨架**:每个项目边界只建一套 `flow/`(`charter.md` `plan.md` `进展.md` `decisions.md` `踩坑记录.md` `tasks/` `history/` `trash/`)+ `docs/`。
5. **生成分层入口**:用 `assets/templates/AGENTS.md` 生成根级 `AGENTS.md`,再让根级 `CLAUDE.md` 指向它。单仓多子项目中，每个有独立职责的子项目用 `assets/templates/MODULE_AGENTS.md` 生成局部 `AGENTS.md` 并建立同目录 `CLAUDE.md` 软链(Windows 改复制);局部入口只写本模块职责、命令、约束和根级指针,不复制总体计划。可选 `DESIGN.md` 仅用于设计 / 创意工作。
6. **装 hook**:只在每个项目边界根目录复制 `assets/templates/.hooks/`、`.claude/`、`.codex/`;`chmod +x .hooks/stop-doccheck.sh`。Claude Code 只以 `prompt_id`、可解析版本不低于 `0.145.0` 的 Codex 只以 `turn_id` 界定用户回合,每回合自动续跑一次;更旧或未知版本安全放行。
7. **详规随项目**:把 `references/` 下详规复制到根级 `flow/规范/`(自包含)。
8. **跑接入自检**:运行 `bash tests/test-stop-hook.sh` 与 `bash tests/test-multi-project-structure.sh`。

### 操作 B: 收工交接 (进展日志)
把当前会话这一棒的进展追加到 `flow/进展.md` 最上面。每条包含：做了什么 / 为什么 / 怎么理解 / 产出(真实路径)/ 问题→解决 / 下一步。

### 操作 C: 任务状态流转与即时物理回收 (Task GC)
1. 任务完成 -> 状态置为 `[-] 待验收` 并输出【提请验收卡】；
2. 用户验收通过 -> 状态置为 `[✓] 已完成` -> **立即物理剪切移入 `flow/history/`**；
3. 用户验收不通过 -> 状态置为 `[✕] 不合格` -> 立即作为当前会话 P0 任务修复。

### 操作 D: 自动热同步与版本接管 (Auto Hot-Sync)
每次会话开工时，比对 `flow/规范/VERSION`。若发现版本落后，自动执行无感增量热同步，并写入最新版本号。

---

## 一直要守的铁律
1. **用户即时指令第一优先级**: 当前对话明确诉求 > 历史 plan。
2. **待验收 `[-]` 绝对不重跑**: 上轮已交付项静默等待用户确认，绝不自我感动式重复排查。
3. **不合格 `[✕]` 立即就地修复**: 用户指出问题，以最高优先级当场改到位。
4. **已完成 `[✓]` 物理零留存**: 验收通过即剪切移出活跃区，保持 plan.md 极致清爽。
5. **按需加载与动态销账**: 不读无关历史，多要点指令做完即销账，一会话一焦点。
6. **自动决策默认、高风险人工裁决**: 常规开发自主推进，仅极高风险或用户指令触发人工决策门。
7. **版本热同步无感接管**: 全局 Skill 更新后，项目在触发时自动完成规范热同步。
8. **一个项目边界一个控制面**: 单仓多子项目只保留一套根级 `flow/`、`docs/` 和 hook，不在子项目机械复制。
9. **目录归属**: 协调 / 推进项目的 → 根级 `flow/`;跨模块知识、方案和交付说明 → 根级 `docs/`;代码 → 对应子项目目录。
